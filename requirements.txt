# Core ML Libraries
pandas==1.3.5            # Stable with built-in containers
scikit-learn==0.24.2     # Supported by SageMaker built-in containers

# AWS SDK
boto3==1.24.0            # AWS SDK for S3 and SageMaker
sagemaker==2.149.0       # SageMaker SDK for pipeline creation

# Utility Libraries (Optional)
colorlog==6.6.0          # Optional for better logging

setuptools==59.5.0  # Avoids `numpy.distutils` deprecation issues
wheel==0.37.1
