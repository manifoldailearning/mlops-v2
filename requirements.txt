# Core ML Libraries
pandas==2.1.2               # Compatible with Python 3.11
numpy==1.24.3               # Works with scikit-learn and Pandas
scikit-learn==1.2.2         # Works with Python 3.11 and Pandas 2.1.x

# Cython and Build Tools (Avoid pyproject.toml Errors)
Cython==0.29.36             # Compatible with scikit-learn builds
setuptools==67.8.0          # Avoids `numpy.distutils` deprecation
wheel==0.40.0               # Required for building C extensions

# AWS SDKs
boto3==1.26.158             # Latest for working with AWS
sagemaker==2.159.0          # Latest for SageMaker pipelines

# Utility Libraries
colorlog==6.7.0             # Optional for colored logs
pydantic
pydantic_core
